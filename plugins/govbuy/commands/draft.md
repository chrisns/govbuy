---
description: Draft procurement scaffolding — timetable, MEAT evaluation matrix, spec outline, compliance steps
argument-hint: <need or RM reference> [further-competition | direct-call-off | direct-award]
---

The user wants to start the procurement documents for: **$ARGUMENTS**

Call the govbuy `draft` tool. Pass an `rm_reference` if they named one (e.g. RM1557.14), else the `need`; pass
`route` if they specified one (`framework_call_off`, `further_competition`, or `direct_award`). Then present:

- **The `scaffolding_only` banner first** — this is editable scaffolding to START the documents, not the documents
  themselves, not legal advice, and every `<<placeholder>>` must be completed and reviewed.
- The **chosen route** and the **dated procurement timetable** (working days from today) as a step list.
- The **evaluation matrix** as a table — flag every `<<placeholder>>` weighting/criterion to complete.
- The **specification outline** and the **compliance checklist** verbatim.
- For a **direct award**: foreground the Schedule 5 justification scaffold + s.44 transparency-notice fields, and
  that a direct award is the exception (lawful only on a genuine Schedule 5 ground), never the default.

Never call a framework call-off a statutory direct award. Link the instrument's `official_url`.
